/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
    domains: ["mbbmnygwewvjsxsjtzbo.supabase.co"],
  },
};

module.exports = nextConfig;
